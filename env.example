# Resend configuration
# Copy this file to `.env` (which is git-ignored) and fill in the values.

# Required: Resend API key with `re_` prefix.
CALAYA_RESEND_API_KEY=your_resend_api_key_here

# Recommended: Verified sender email address.
CALAYA_RESEND_FROM=noreply@calayaengineering.com

# Optional: Fallback sender address (must be verified in Resend).
CALAYA_RESEND_FROM_FALLBACK=onboarding@resend.dev

# Optional: Comma-separated list of recipients for internal notifications.
CALAYA_RESEND_TO=info@calayaengineering.com,calayaengineering@yahoo.co.uk

# NOTE:
# - Never commit real secrets to the repository.
# - Update `.env` or your deployment environment with the actual credentials.
